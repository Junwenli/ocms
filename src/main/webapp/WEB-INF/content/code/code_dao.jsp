package ${packageName}.repository.${module};

import com.linkcm.core.dao.BaseRepository;
import ${packageName}.entity.${name};

public interface ${name}Repository extends BaseRepository<${name}, ${idType}> {

}